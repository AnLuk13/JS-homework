//Request two numbers, divide them and the result must be an integer(whole number)

let firstNumber = parseInt(prompt("Enter the first number"));
let secondNumber = parseInt(prompt("Enter the second number"));
alert("The result of the devision " + Math.trunc(firstNumber / secondNumber));
